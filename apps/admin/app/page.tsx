// Separate deployment, separate staff auth, separate database role (PRD-13,
// register decision A18). Built in step 5.1 of the build plan; this is a
// placeholder so the workspace structure exists from day one.
export default function AdminHomePage() {
  return <main>ProCircuit Admin — build in progress</main>;
}
