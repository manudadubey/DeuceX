import { PlaceholderPage } from '@/components/shell/placeholder-page';

export default function TournamentAgentPage() {
  return (
    <PlaceholderPage description="Tournament shortlist, entry deadlines, budget-to-go and expected value per event." />
  );
}
