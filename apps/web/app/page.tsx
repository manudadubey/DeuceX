// Placeholder route. The real shell (sidebar, topbar, mobile tab bar, hash
// routing ported to Next.js routing) lands in build plan step 0.5.
export default function HomePage() {
  return <main>ProCircuit — build in progress</main>;
}
